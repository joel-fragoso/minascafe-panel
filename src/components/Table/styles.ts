import styled from 'styled-components';

export const Container = styled.div`
  overflow-x: auto;

  table {
    display: table;
    width: 100%;
    border-spacing: 0 0.8rem;
    margin-top: 1.6rem;
  }
`;
